mixin radioColumn(name, items)
    //-  name = 'grades'
    //-  items = ['Infant', 'Toddler', 'PreK 3', 'PreK 4', 'K']
    .form__radio-column
        for item in items
            -var itemValue = item.replace(' ', '')
                .form__radio-item   
                    input(id=itemValue type='radio' name=name value=itemValue required)
                    label.form__label-radio(for=itemValue)= item